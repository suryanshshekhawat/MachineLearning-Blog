[
  {
    "id": "fl-secagg-dp-thesis",
    "title": "Privacy-Preserving Federated Learning with Secure Aggregation",
    "date": "2026-09-01",
    "summary": "Undergraduate thesis: federated learning + secure aggregation + differential privacy, applied to wind farm SCADA telemetry. Small interactive diagrams build up to the real protocol visualiser, embedded live.",
    "blocks": [
      { "type": "text", "html": "<h1>Building a Privacy-Preserving Federated Learning System with Secure Aggregation</h1><p class=\"project-meta\">Undergraduate thesis: Bachelor of Engineering (Honours), University of Sydney, MINT Research Group. Supervised by Dr. Ali Shakiba.</p><p>Wind farms don't want to share their sensor data with each other, and honestly, fair enough: it's commercially sensitive. So this thesis builds a system that trains one shared model across several wind farms' data without any of that data ever leaving home, then goes a step further so the training process itself can't be watched either.</p>" },

      { "type": "tldr", "items": [
        "Wind farm operators each sit on rich turbine sensor data, but competitive and legal reasons keep it siloed. Individually too small to train much, collectively plenty.",
        "The system: Federated Learning trains across farms without moving the data, Secure Aggregation stops the server from peeking at anyone's individual update, and Differential Privacy adds a formal guarantee on top if you want it.",
        "Secure Aggregation turned out to be free: same accuracy as plain federated training. Differential Privacy is not free; it's a real and fairly steep cost, measured below.",
        "Three small diagrams let you break each idea yourself before the actual protocol visualiser runs all three together on real data, at the end."
      ] },

      { "type": "chapter", "number": "00", "id": "why", "title": "Why this exists" },

      { "type": "text", "html": "<p>Wind power is now a serious part of the global energy mix. Installed capacity passed <a href=\"https://wwindea.org/GlobalStatistics\" target=\"_blank\" rel=\"noopener\">1,347 GW at the end of 2025, meeting over 11% of world electricity demand</a>, and it's <a href=\"https://www.iea.org/reports/renewables-2025\" target=\"_blank\" rel=\"noopener\">forecast to nearly double by 2030</a>. Keeping that fleet running isn't cheap either: <a href=\"https://www.wind-energy-the-facts.org/operation-and-maintenance-costs-of-wind-generated-power.html\" target=\"_blank\" rel=\"noopener\">operations and maintenance account for roughly 20&ndash;25% of the levelised cost of wind energy</a>, so better fault-detection models are a fairly direct lever on the cost of clean electricity.</p><p>The obstacle is data. Every wind farm operator collects rich SCADA (Supervisory Control and Data Acquisition) telemetry from its turbines, but that data almost never leaves the organisation. Competitive sensitivity and regulation like the <a href=\"https://eur-lex.europa.eu/legal-content/EN/TXT/?uri=CELEX:32016R0679\" target=\"_blank\" rel=\"noopener\">GDPR</a> make centralising it a non-starter, so you end up with a pile of data silos, each too small on its own to train a reliable model, but plenty in aggregate.</p><p>What follows trains one shared model across those silos without the raw data ever leaving any of them, using real turbine data from the public CARE benchmark (three wind farms). Each idea below gets its own small diagram first, and the full protocol visualiser, where they all run together on the real data, is waiting at the end.</p>" },

      { "type": "chapter", "number": "01", "id": "fl", "title": "Federated Learning" },

      { "type": "text", "html": "<p>Normally you'd centralise the data and train one model on all of it. Federated Learning flips that around: a shared model gets trained across distributed clients (here, wind farms) without any client's raw data ever leaving its own site.</p><p>Each round works the same way. The server broadcasts the current global model to the clients, each client trains it briefly on its own local data, and sends back only the resulting parameter adjustments, never the data itself. The server combines those, weighted by how much data each client trained on:</p><p class=\"equation\">w<sub>t+1</sub> &larr; &Sigma;<sub>k</sub> (n<sub>k</sub> / n) &middot; w<sub>k</sub><sup>t+1</sup></p><p>In plain English, that just says: scale each farm's new numbers by its share of the total data, then add all those scaled numbers up. A farm holding ten times the data of its peers gets ten times the say in the result, which matters here since wind farms vary in fleet size by an order of magnitude. This scheme is called Federated Averaging, or FedAvg (<a href=\"https://arxiv.org/abs/1602.05629\" target=\"_blank\" rel=\"noopener\">McMahan et al., 2017</a>), and it's the workhorse of the field. The diagram below strips everything else away: three clients, small weight vectors standing in for a full model, one aggregate.</p>" },

      { "type": "iframe", "src": "projects/fl-basics/index.html", "height": 480, "caption": "Click run round a few times and watch each farm's weight vector change and the arithmetic below update. Then take a farm offline and run it again: its row disappears from the sum, and the other two farms' shares grow to make up the difference." },

      { "type": "stat", "value": "K = 3", "label": "wind farms in the CARE benchmark: the entire client population this trains across, and dozens of times smaller than FL was originally built for" },

      { "type": "text", "html": "<p>FL was designed and proven at huge scale: Google's production deployment <a href=\"https://arxiv.org/abs/1902.01046\" target=\"_blank\" rel=\"noopener\">handles roughly 10 million daily active phones</a>. Wind farm operators are the opposite: a handful of identifiable companies, each holding a large, high-quality dataset. That's the cross-silo regime, and it changes which problems actually matter, as the next section shows.</p>" },

      { "type": "chapter", "number": "02", "id": "secagg", "title": "Secure Aggregation" },

      { "type": "text", "html": "<p>\"The data never leaves the site\" turns out to be an incomplete promise. The gradient update each client sends back still carries a lot of information about the data it came from: <a href=\"https://arxiv.org/abs/2003.14053\" target=\"_blank\" rel=\"noopener\">Geiping et al. showed</a> you can reconstruct high-resolution training images from a single gradient vector, and all it takes is a standard honest-but-curious server, meaning one that follows the protocol correctly but reads every message it receives. Keeping data local isn't enough if the updates themselves leak it.</p><p>Secure Aggregation, from <a href=\"https://eprint.iacr.org/2017/281\" target=\"_blank\" rel=\"noopener\">Bonawitz et al.</a>, closes that gap by masking each client's update before it reaches the server. Each client adds a private mask plus a set of pairwise masks it's agreed with every other client via Diffie&ndash;Hellman key exchange:</p><p class=\"equation\">&#375;<sub>u</sub> = w<sub>u</sub> + b<sub>u</sub> + &Sigma;<sub>v&ne;u</sub> s<sub>u,v</sub></p><p>In plain terms: before sending anything, each client adds a random number to its own update (its private mask), plus one shared secret number per other client, agreed on in advance so no one else ever sees it. The result looks like meaningless noise on its own. The pairwise masks are built to cancel out exactly when everything is summed, so the server only ever recovers the aggregate:</p><p class=\"equation\">&Sigma;<sub>u</sub> &#375;<sub>u</sub> = &Sigma;<sub>u</sub> w<sub>u</sub></p><p>In other words: add up everyone's disguised numbers, and every secret cancels against its matching pair, leaving only the true total the server was allowed to see all along. Below, the same three farms mask their small weight vectors before sending them. See exactly what the server receives versus what each farm actually computed.</p>" },

      { "type": "iframe", "src": "projects/secagg-basics/index.html", "height": 280, "caption": "Masking starts on: run a round and watch the \"server receives\" column turn into what looks like noise, yet it still sums correctly. Flip masking off to see the difference, then drop farms (click a name) until fewer than &tau; are left standing: the round aborts instead of revealing anything." },

      { "type": "stat", "value": "τ = 2 of 3", "label": "lose one farm and you're already at the abort boundary: at K=3, dropping a single client removes a third of the network, not a rounding error" },

      { "type": "text", "html": "<p>SecAgg was designed and evaluated at K in the hundreds to millions, where losing a handful of clients barely registers. At K=3, this thesis's first research question is basically whether SecAgg's guarantees hold up gracefully at a client count this small, or whether something breaks.</p>" },

      { "type": "chapter", "number": "03", "id": "dp", "title": "Differential Privacy" },

      { "type": "text", "html": "<p>SecAgg solves one problem, the server never sees any individual update, but it says nothing about what the aggregate itself reveals. That's what Differential Privacy is for: a formal guarantee that a mechanism's output looks nearly the same whether or not any single record was included. That's a claim about whether an observer can tell two datasets apart, not just \"the numbers got noisier.\" The diagram below is trying to show that difference directly.</p>" },

      { "type": "iframe", "src": "projects/dp-basics/index.html", "height": 400, "caption": "At low &sigma;, release a dozen samples and the two clusters stay apart: you can mostly tell which dataset produced which point. Drag &sigma; up and release more. Once the clusters blur together, that loss of distinguishability is the actual privacy guarantee, not just the number moving around." },

      { "type": "text", "html": "<p>In practice this means adding calibrated noise. The usual recipe, <a href=\"https://arxiv.org/abs/1607.00133\" target=\"_blank\" rel=\"noopener\">DP-SGD</a>, clips each client's update to a bounded norm C and adds Gaussian noise before aggregation:</p><p class=\"equation\">noise ~ N(0, &sigma;&sup2;C&sup2;)</p><p>In plain terms: first shrink every update so no single one can be too large or too influential (that's the clipping), then sprinkle in random noise whose size is controlled by &sigma;. Bigger &sigma; means more noise piled on top, which is exactly why it buys more privacy at a cost to accuracy, and there's no way around that trade-off. It's also worth being clear that SecAgg and DP aren't substitutes for each other: SecAgg hides who sent what, DP limits how much any one record could have moved the result.</p>" },

      { "type": "stat", "value": "20× worse", "label": "mean RMSE at σ=2.0 versus no noise at all, measured on this thesis's own prototype" },

      { "type": "image", "src": "projects/thesis-assets/dp_tradeoff.png", "caption": "Measured privacy-utility trade-off: RMSE against DP noise multiplier &sigma;, averaged across the three real CARE wind farms." },

      { "type": "image", "src": "projects/thesis-assets/convergence.png", "caption": "Measured convergence: FedAvg alone and FedAvg through SecAgg land within six decimal places of each other. The masking recovers exactly the same aggregate." },

      { "type": "chapter", "number": "04", "id": "results", "title": "Stitching it together" },

      { "type": "text", "html": "<p>There's mature theory for SecAgg and plenty of prior work applying FL to industrial sensor data, but nothing that puts both together on a real regression task at the client counts a wind-energy deployment would actually have. That's the gap, and it leads to two questions:</p><p>RQ1: how can Secure Multiparty Computation, and Secure Aggregation specifically, be integrated into a federated learning system to protect updates during aggregation?</p><p>RQ2: how much individual privacy do current secure aggregation protocols actually hold onto, once you test them on a real regression task with wind turbine SCADA data?</p><p>A first, deliberately small prototype (one real CARE event file per farm, a small MLP, 25 FedAvg rounds) gives an early read before scaling up to the thesis's full empirical chapter:</p>" },

      { "type": "table", "headers": ["Scenario", "Farm A", "Farm B", "Farm C", "Mean"], "rows": [
        ["Centralised", "0.060", "0.046", "0.079", "0.062"],
        ["FedAvg", "0.105", "0.080", "0.083", "0.089"],
        ["FedAvg + SecAgg", "0.105", "0.080", "0.083", "0.089"],
        ["FedAvg + SecAgg + DP (&sigma;=0.1)", "0.093", "0.102", "0.094", "0.096"],
        ["FedAvg + SecAgg + DP (&sigma;=0.5)", "0.238", "0.171", "0.090", "0.166"],
        ["FedAvg + SecAgg + DP (&sigma;=2.0)", "2.410", "1.800", "1.941", "2.050"]
      ], "note": "RMSE in kW, the CARE dataset's normalised power units." },

      { "type": "text", "html": "<p>A few things jump out. SecAgg really does cost nothing here: the FedAvg and FedAvg+SecAgg rows match to six decimal places, so aggregation privacy comes for free, and now that's shown on a regression task rather than the classification benchmarks the published SecAgg papers rely on. Keeping the data siloed at all costs more than SecAgg does: FedAvg alone is 30 to 70% worse than pooling everything centrally, which is really just the price of three physically different sites (their average temperatures alone span 20&deg;C, 12&deg;C and 29&deg;C) staying separate. DP is where the real cost lives, and it's steep even at moderate noise. And small K makes dropout matter fast: with three clients and a survival threshold of two, losing one farm for a round is already close to the edge. In a 20-round dropout test, one bad round with two farms down triggered an outright abort.</p><p>Worth being upfront that this is a prototype, not the thesis's final empirical result. It trains on one ~4,000-row event file per farm rather than the full 95-file, ~19GB CARE corpus, and SecAgg's arithmetic is reproduced faithfully without an actual Diffie&ndash;Hellman exchange over a network. Its job is to sanity-check the pipeline before scaling up, which it did.</p>" },

      { "type": "chapter", "number": "05", "id": "try-it", "title": "Try it yourself" },

      { "type": "text", "html": "<p>Everything above, in one place: the actual protocol visualiser built for this thesis, with federated rounds, dropout, Secure Aggregation and DP noise all running together on the real three-farm CARE topology, plus the measured RMSE curves available to overlay against its own live illustrative one.</p>" },

      { "type": "iframe", "src": "projects/secagg-sandbox/index.html", "height": 620, "caption": "Every control unlocked at once: federated rounds, dropout, Secure Aggregation, DP noise, on the real CARE wind-farm topology." },

      { "type": "collapsible-pdf", "label": "Read the full literature review (36 pages)", "src": "downloads/thesis-literature-review.pdf" }
    ]
  }
]
